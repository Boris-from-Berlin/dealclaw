version: '3.8'
services:
  api:
    build: ./mvp
    ports:
      - "4000:3000"
    environment:
      NODE_ENV: production
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
    volumes:
      - dealclaw-data:/app/data

volumes:
  dealclaw-data:
